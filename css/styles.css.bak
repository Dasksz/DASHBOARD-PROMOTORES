        border-bottom: 1px solid rgba(255, 255, 255, 0.05) !important;
        min-height: auto !important;
    }

    #mix-view td[data-label="Categorias"] {
        text-align: center !important;
        width: 3rem; /* Fixed width for counter column */
        vertical-align: middle;
    }

    /* Hide hidden columns strictly */
    #mix-view .hidden {
        display: none !important;
    }

    /* Ensure Header Visibility */
    #mix-view thead tr {
        background-color: rgba(30, 41, 59, 0.8) !important;
    }
}

/* --- META REALIZADO COMPACT VIEW (MOBILE) --- */
@media (max-width: 768px) {
    #metaRealizadoTableContainer,
    #metaRealizadoClientsTableContainer {
        display: table !important;
        width: 100%;
        border-collapse: separate;
        border-spacing: 0 0.5rem; /* Spacing between "cards" */
    }

    #metaRealizadoTableContainer thead,
    #metaRealizadoClientsTableContainer thead {
        display: none !important;
    }

    #metaRealizadoTableContainer tbody,
    #metaRealizadoClientsTableContainer tbody {
        display: table-row-group !important;
    }

    #metaRealizadoTableContainer tr,
    #metaRealizadoClientsTableContainer tr {
        display: block;
        margin-bottom: 0.5rem;
        background-color: rgba(15, 23, 42, 0.6) !important;
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 0.75rem;
        padding: 0.75rem;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        cursor: pointer; /* Indicate clickability */
        transition: background-color 0.2s;
    }

    #metaRealizadoTableContainer tr:active,
    #metaRealizadoClientsTableContainer tr:active {
        background-color: rgba(30, 41, 59, 0.8) !important;
        transform: scale(0.98);
    }

    #metaRealizadoTableContainer td,
    #metaRealizadoClientsTableContainer td {
        display: block !important;
        padding: 0 !important;
        border: none !important;
        text-align: left !important;
        min-height: auto !important;
    }

    /* Hide standard pseudo-labels */
    #metaRealizadoTableContainer td::before,
    #metaRealizadoClientsTableContainer td::before {
        content: none !important;
        display: none !important;
    }

    /* Target specific cells for layout */
    /* Assume JS will output specific classes or we use nth-child carefully */
    /* Since we are rewriting the JS render function, we can simplify CSS by relying on classes there. */
    /* But if we stick to the plan of "Compact List", the JS will render a SINGLE cell or simpler structure. */
    /* Let's assume the JS will render 2 lines within a single TD or minimal structure. */
}
