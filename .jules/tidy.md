## 2024/05/18 : (Repeated Filter Rendering Logic - Rede & TipoVenda) **Aprendizado:** UI modules extensively duplicated filter rendering logic for 'Rede' and 'TipoVenda', iterating directly via Proxy `.get()` causing GC pressure in ColumnarDatasets. **Ação:** Refactored into reusable globally exposed logic `window.updateGenericRedeFilter` and `window.updateGenericTipoVendaFilter` that implements direct columnar checks (`typeof dataSource.get === 'function' && dataSource._data`) to bypass Proxy allocation overhead, avoiding repetitive mapping implementations across different components.

## 2024-06-25 : (Generic Filter Handlers Refactoring) **Aprendizado:** Many `setup[View]SupervisorFilterHandlers` functions were heavily duplicated across `js/app/app.js`, consisting of almost identical DOM manipulation code for showing/hiding dropdowns, updating selected sets, calling UI updates, and registering click-away listeners. This added huge amounts of technical debt. **Ação:** Refactored by extracting the shared logic into a global `window.setupGenericFilterHandlers` in `js/app/utils.js` which handles button clicks, dropdown changes, global click listeners, and function callbacks. Applied this utility backwards to replace over 10 duplicated functions in `app.js`.

## 2024-08-01 : (View-Specific Filter Wrapper Refactoring)
**Aprendizado:** Even after extracting logic to `window.setupGenericFilterHandlers`, large blocks of code calling this method with 9 standard arguments were duplicated across 11 different setup functions in `js/app/app.js` (e.g. `setupCitySupervisorFilterHandlers`, `setupMixSupervisorFilterHandlers`). This caused boilerplate bloat.
**Ação:** Implemented a single `setupViewSupervisorFilters(config)` factory wrapper within `js/app/app.js` that constructs the call and defaults optional parameters (`onClose`, `isSellerFilter`), then migrated all 11 instances to use it. When doing structural search-and-replace, carefully preserve line endings (CRLF vs LF) to avoid unwanted whitespace diffs in git.
